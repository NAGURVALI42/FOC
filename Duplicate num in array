#include<stdio.h>
int main()
{
	int a[50],i,j,n;
	printf("Enter the n value:");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		printf("Elements are %d=",i+1);
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		for(j=i+1;j<n;j++)
		{
		if(a[i]==a[j])
		printf("Duplicate number:%d\t",a[j]);
	}
	}
	return 0;
}
