#include<stdio.h>
int main()
{
	int a,b,c,n;
	printf("Enter the n value:");
	scanf("%d",&n);
	for(a=1;a<=n;a++)
	{
		for(b=a;b<=n;b++)
		{
		for(c=b;c<=n;c++)
		{
			if(a*a+b*b==c*c){
				printf("(%d,%d,%d)\n",a,b,c);
			}
			}	
		}
	}
	return 0;
}
