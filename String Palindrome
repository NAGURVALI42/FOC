#include<stdio.h>
#include<string.h>
int main()
{
	char a[10],b[10];
	printf("Enter the string:");
	gets(a);
	strcpy(b,a);
	strrev(b);
	if(strcmp(a,b)==0)
	printf("It is palindrome");
	else
	printf("It not  palindrome");
	return 0;
}
