#include<stdio.h>
int main()
{
	int i,j,k,n;
	int c=1;
	printf("Enter the n value:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
		{
			for(j=0;j<=n-i;j++)
			{
				printf(" ");
			}
		for(k=0;k<=n;k++){
			c=c*(i-j)/(j+1);
			printf("%d",c);
		}
		printf("\n");
	
	}
	}
