#include<stdio.h>
int main()
{
	int i,n,a[50],sum=0;
	double avg;
	printf("Enter the n value:");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		printf("Element %d:",i+1);
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		sum=sum+a[i];
		avg=sum/n;
	}
	printf("Average :%.2lf",avg);
	return 0;
}
